# 山海灵境素材

`spirit-forest.png`：本次生成的青绿古林背景。
`spirit-atlas.png`：本次生成的三级古树和墨灵透明图集，由渲染器按已检查边界读取并进行阵营着色。
`brush.ttf`：Ma Shan Zheng，授权见 `FONT-LICENSE.txt`。

当前游戏未嵌入《墨战》的宣传截图或游戏素材。旧的 `battlefield.png` 和 `ink-atlas.png` 属于城战过渡版本，未进入本次交付包，也不参与当前渲染。
